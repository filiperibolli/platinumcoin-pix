package com.platinumcoin.pix.common;

/**
 * Placeholder so common-lib has a compilable source from day one.
 * Replaced by real shared code (error model, JWT, logging) in later steps.
 */
public final class Placeholder {

    private Placeholder() {
    }

    /** Marker used by the wiring probe test. */
    public static boolean ready() {
        return true;
    }
}
